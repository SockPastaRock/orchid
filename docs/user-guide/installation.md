# Installation

```bash
make build    # compiles release binary to ./bin/orchid
```

Add `./bin` to your `PATH`, or copy the binary to a directory already on it:

```bash
cp bin/orchid /usr/local/bin/orchid
```
