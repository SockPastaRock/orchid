# Hooks

> **Not implemented.** The hooks system is not yet available. The `orchid hook` command does not exist. The `hooks` field in `metadata.json` is accepted but silently ignored.

Check [architecture/hooks.md](../architecture/hooks.md) for implementation status.
